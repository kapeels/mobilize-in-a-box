# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|

  config.vm.box = "ohmage/mobilize-in-a-box"
  config.vm.network "private_network", ip: "192.168.33.100"
  config.vm.hostname = "mobilize-in-a-box"

end