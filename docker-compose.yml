nginx:
  restart: unless-stopped
  image: mobilizingcs/nginx
  links:
  - ohmage:ohmage
  ports:
  - "80:80"

ohmage:
  restart: unless-stopped
  image: ohmage/base
  links:
  - mysql:mysql

mysql:
  restart: unless-stopped
  image: mysql:5.6.27
  environment:
  - MYSQL_ROOT_PASSWORD=setmebetter
